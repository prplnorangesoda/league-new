{
  "name": "league-backend",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --compile --minify --sourcemap --bytecode --outfile=app"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.4"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
